import numpy as np

total_possible = 440
hw = np.array([1170, 163, 188, 150])
quiz = np.array([45, 40, 50, 36])
test = np.array([160, 195, 138, 172])
print(quiz)
print(type(test))
print(hw.ndim)
final = (hw + quiz + test) / total_possible
print(final)
hw = np.append(hw, 150)
print(hw)
a = np.linspace(0, 10, 20)  # np.linspace(start, stop, number)
print(a)
b = np.arange(0, 15, 5)  # np.arange(start, stop, step)
print(b)
c = np.array([1, 2, 3, 4])
print(c)
c = np.reshape(c, (1, 4))
print(c)
c = np.reshape(c, (4, 1))
print(c)
c = np.reshape(c, (-1, 1))  # -1 will be replaced by length
print(c)
c = np.reshape(c, (2, 2))
print(c)
d = np.array([1, 2, 3, 4])
print('original', d)
d = np.transpose(d)
print('transposed', d)

print('???????????????????????????????????????????????????????????????')
print("A1 is currently a column vector:")
A1 = np.arange(1, 4)
print(A1)
print("We can make it into a row vector with the transpose function:")
A1 = np.transpose(A1)
print(A1)
print("And then we can make it back into a column vector:")
A1 = np.transpose(A1)
print(A1)
print('???????????????????????????????????????????????????????????????')
